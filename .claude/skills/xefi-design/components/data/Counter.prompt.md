Incrémenteur numérique — pour une petite quantité entière que l'utilisateur ajuste pas à pas.

```jsx
<Counter value={nb} min={1} max={20} onChange={setNb} />
```

Au-delà d'une vingtaine de valeurs, la saisie directe est plus rapide : utilisez `Input`.