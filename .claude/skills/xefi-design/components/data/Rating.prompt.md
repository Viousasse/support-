Notation par étoiles — pour une appréciation saisie ou affichée.

```jsx
<Rating value={4} max={5} onChange={setNote} />
<Rating value={4} readOnly size={16} />
```

`readOnly` pour l'affichage seul. La note n'est jamais la seule information : accompagnez-la du nombre d'avis ou d'un libellé.