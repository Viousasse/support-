Touche de clavier — pour documenter un raccourci dans une aide ou une info-bulle.

```jsx
<Kbd combo="Ctrl+S" />
<Kbd>Entrée</Kbd>
```

`combo` découpe automatiquement sur `+` et rend une touche par segment.