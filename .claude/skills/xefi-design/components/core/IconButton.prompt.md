Bouton carré icône seule, même hauteur que les champs voisins.

```jsx
<IconButton icon={<Icon name="pencil" />} label="Modifier" size={36} />
```

`variant="ghost"` retire le fond et la bordure (usage en ligne de tableau).
